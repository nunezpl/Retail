package com.ecommerce.backend.client;

// @FeignClient(name = "bankClient", url = "http://banco-abc.com/api")
public interface BankClient {
    // @PostMapping("/pagos")
    // PagoResponse realizarPago(@RequestBody PagoRequest pago);
}
