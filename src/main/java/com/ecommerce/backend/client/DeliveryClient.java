package com.ecommerce.backend.client;

// @FeignClient(name = "deliveryClient", url = "http://delivery-abc.com/api")
public interface DeliveryClient {
    // @PostMapping("/envios")
    // void registrarEnvio(@RequestBody EnvioRequest envio);
}
